//write a java script program to find the length of your given string SAMPADHA
// Define the string
const str = "SAMPADHA";

// Calculate the length of the string
const length = str.length;

// Print the length of the string
console.log("The length of the string '" + str + "' is: " + length);